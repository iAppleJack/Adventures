<% if !flash[:notice].blank? %>
    <div class = "alert alert-info">
      <%= flash[:notice] %>
    </div>
<% end %>

<br />

<%= link_to "New Resume", travels_new_path, class: "btn btn-primary" %>
    <br />
    <br />

    <table class = "table table-bordered table-striped">
      <thead>.
      <tr>
        <th>Name</th>
        <th>Country</th>
        <th> Image</th>
      </tr>
      </thead>

      <tbody>
      <% @travels.each do |travel| %>

          <tr>
            <td><%= travel.name %>
            <%= travel.country %>

            <%= image_tag  travel.img_url , :width => 320 , :height => 240 %></td>
      </tr>

              <% end %>
    </tbody>

              </table>